#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")" && pwd)"
cd "$ROOT"

# بوابة تسليم مختصرة: فحوص المخاطر الأساسية ثم بناء المنصة الحالية فقط.
npm ci
npm run test:delivery
npm run tauri build

echo "GO: بُنيت نسخة التسليم للمنصة الحالية بنجاح."
