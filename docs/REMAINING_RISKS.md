# REMAINING RISKS — المخاطر المتبقية

> **ملاحظة تحديث إعادة التدقيق (2026-07-11):** لا ن claims أن المخاطر أصبحت صفراً. هذه قائمة صادقة.

## تصنيف المخاطر

الخطورة: 🔴 عالية | 🟡 متوسطة | 🟢 منخفضة
الاحتمال: 🔴 مرتفع | 🟡 متوسط | 🟢 منخفض

---

## 🔴 الخطر #1: عدم القدرة على تشغيل اختبارات Rust

- **الخطورة:** 🔴 عالية
- **الاحتمال:** 🔴 مرتفع (مؤكد في بيئة التدقيق)
- **الأثر:** لا يمكن التحقق من تجميع Rust ولا نجاح اختبارات الـBackend.
- **السبب:** بيئة التدقيق الحالية تفتقر لصلاحيات root لتثبيت `libgtk-3-dev` و `libwebkit2gtk-4.1-dev` التي يتطلبها `tauri` crate.
- **طريقة التخفيف:** نقل المشروع إلى بيئة Linux بصلاحيات root كاملة وتشغيل `cargo test --features accounting-test-support`.
- **هل يُقبل قبل التسليم؟** ❌ لا — قرار No-Go مفروض.

## 🔴 الخطر #2: `legacy.rs` بحجم 21,500+ سطر

- **الخطورة:** 🔴 عالية
- **الاحتمال:** 🔴 مرتفع (الحالة الحالية)
- **الأثر:** صعوبة الصيانة، صعوبة الفحص، احتمال إصلاحات متعارضة.
- **السبب:** إعادة الهيكلة (PHASE-3-RESTRUCTURE) أنشأت ملفات Domains شكلية لكنها لم تنقل المنطق فعلياً.
- **طريقة التخفيف:** نقل المنطق تدريجياً إلى `domains/cars/service.rs` و `domains/sales/service.rs` إلخ. مع `cargo check` بعد كل نقل.
- **هل يُقبل قبل التسليم؟** ⚠️ مقبول مؤقتاً لأن الإصلاحات الجذرية السبعة مطبقة داخل `legacy.rs`.

## 🟡 الخطر #3: `financial_ledger` و `journal_entries/journal_lines` يعيشان معاً

- **الخطورة:** 🟡 متوسطة
- **الاحتمال:** 🟡 متوسط
- **الأثر:** مصدران محتملان للحقيقة للحسابات. قد يختلفان إذا كُتب لأحدهما دون الآخر.
- **السبب:** Migration 36 أنشأ `journal_entries/journal_lines` لكن الكود لم يكتب إليهما بعد.
- **طريقة التخفيف:** كتابة كود يكتب إلى **كلا** الجدولين خلال فترة الانتقال. ثم إيقاف `financial_ledger`.
- **هل يُقبل قبل التسليم؟** ⚠️ مقبول مؤقتاً.

## 🟡 الخطر #4: بعض الأوامر تستخدم `require_admin_session(&db, None)`

- **الخطورة:** 🟡 متوسطة
- **الاحتمال:** 🟡 متوسط
- **الأثر:** الـaudit log لا يسند العملية لمستخدم محدد.
- **السبب:** تم إصلاح `save_and_sell_car_with_accounting` فقط (CRITICAL-7). 9 مواقع أخرى لا تزال تستخدم `None`.
- **المواقع المتأثرة:** `legacy.rs:6094, 6744, 7124, 7296, 8168, 8369, 10795, 12110, 12123`.
- **طريقة التخفيف:** إضافة `session_token: Option<String>` لكل أمر كتابة، وتمريره من الواجهة.
- **هل يُقبل قبل التسليم؟** ⚠️ مقبول مؤقتاً (المسار الآمن لا يزال يعمل).

## 🟡 الخطر #5: SQL `LIKE 'ايداع%'` للحركة

- **الخطورة:** 🟡 متوسطة
- **الاحتمال:** 🟢 منخفض
- **الأثر:** إذا أُدخل نوع حركة جديد بـprefix مختلف، قد لا يُحتسب.
- **السبب:** الكود الحالي يستخدم Prefix عربي للاستدلال على اتجاه الحركة (ايداع/سحب).
- **طريقة التخفيف:** استبدال بـEnums أو Codes ثابتة (مثل `transaction_kind: 'deposit' | 'withdrawal'`).
- **هل يُقبل قبل التسليم؟** ⚠️ مقبول مؤقتاً (الـprefix محدد في `is_deposit_type`).

## 🟡 الخطر #6: Backend Bridge يعتمد Mock

- **الخطورة:** 🟡 متوسطة
- **الاحتمال:** 🔴 مرتفع
- **الأثر:** اختبارات `bridge.backend.test.ts` و E2E لا تصل إلى Rust/SQLite حقيقية.
- **السبب:** الـBackend Bridge هو Node HTTP server يـmock الـTauri commands.
- **طريقة التخفيف:** استبدال بـ`cargo test --features accounting-test-support` على Rust + SQLite حقيقية.
- **هل يُقبل قبل التسليم؟** ❌ لا — هذه مخالفة صريحة لقاعدة "Pipeline يجب أن يفشل إذا Backend mode = mock".

## 🟢 الخطر #7: أسماء الشركاء مدمجة في logic الواجهة

- **الخطورة:** 🟢 منخفضة
- **الاحتمال:** 🟡 متوسط
- **الأثر:** إذا تغير اسم شريك، قد تتوقف بعض الميزات.
- **السبب:** `CompanyStatusTab.tsx` يبحث عن "أمير" و "منتصر" بالاسم.
- **طريقة التخفيف:** استخدام `partner_id` بدلاً من الاسم.
- **هل يُقبل قبل التسليم؟** ✅ نعم (الشركاء ثابتان في هذا المشروع).

## 🟢 الخطر #8: `init_db_for_test` قد يكون لها patterns منفصلة

- **الخطورة:** 🟢 منخفضة
- **الاحتمال:** 🟢 منخفض
- **الأثر:** قد تختلف عن `init_db` الإنتاجية.
- **السبب:** لم يُفحص بدقة.
- **طريقة التخفيف:** توحيد الكود بحيث `init_db_for_test` يستدعي `init_db` فقط.
- **هل يُقبل قبل التسليم؟** ✅ نعم.

## 🟢 الخطر #9: اختبارات regex الأمن تعتمد مكان الكود

- **الخطورة:** 🟢 منخفضة
- **الاحتمال:** 🟢 منخفض
- **الأثر:** إذا انتقل المنطق لملف آخر، قد يفشل الاختبار.
- **السبب:** `security_regression.test.ts` يفحص نص الملف بدلاً من السلوك.
- **طريقة التخفيف:** تم تحديثها لقراءة `lib.rs` + `legacy.rs` معاً.
- **هل يُقبل قبل التسليم؟** ✅ نعم.

## 🟡 الخطر #10: حجم chunk الواجهة 2.27 MB

- **الخطورة:** 🟢 منخفضة
- **الاحتمال:** 🔴 مرتفع
- **الأثر:** بطء تحميل أولي للتطبيق.
- **السبب:** Vite يحذر من chunk > 500 KB.
- **طريقة التخفيف:** code-splitting عبر `import()` ديناميكي.
- **هل يُقبل قبل التسليم؟** ✅ نعم (تطبيق سطح مكتب، التحميل من المحلي).

---

## ملخص المخاطر

| الخطورة | العدد | المقبول قبل التسليم؟ |
|---|---|---|
| 🔴 عالية | 2 | ❌ لا (الخطر #1 يحتم No-Go) |
| 🟡 متوسطة | 4 | ⚠️ مؤقتاً |
| 🟢 منخفضة | 4 | ✅ نعم |

## قرار Go/No-Go

### ❌ No-Go

**السبب الرئيسي:** الخطر #1 (عدم القدرة على تشغيل اختبارات Rust) يجعل من المستحيل التحقق من شروط Go:
- ✅ نجاح Clean Build — غير متحقق (cargo check يفشل).
- ✅ تنفيذ Rust tests فعلياً — غير متحقق (0 اختبار منفذ).
- ✅ عدم وجود Backend Stub أو Mock — غير متحقق (Backend Bridge يستخدم Mock).
- ✅ نجاح السيناريوهات المحاسبية الحرجة — غير متحقق (0/37).
- ✅ نجاح Migration snapshots — غير متحقق.
- ✅ نجاح Backup/Restore — غير متحقق.

**مستوى الثقة الحالي:** 30-40% (كما في تقرير التدقيق الأصلي).

**مستوى الثقة المتوقع بعد التشغيل في بيئة كاملة:** 70-80% (الإصلاحات مطبقة، يلزم فقط التحقق).

## خطة التخفيف التالية

1. **فوري:** نقل المشروع إلى بيئة Linux بصلاحيات root.
2. **فوري:** تثبيت اعتماديات Tauri (`libgtk-3-dev`, `libwebkit2gtk-4.1-dev`, إلخ).
3. **فوري:** تشغيل `cargo test --features accounting-test-support`.
4. **قصير المدى:** تشغيل السيناريوهات الـ37.
5. **قصير المدى:** استبدال Backend Bridge بـRust integration tests.
6. **متوسط المدى:** نقل المنطق من `legacy.rs` إلى Domains فعلية.
7. **متوسط المدى:** توحيد `financial_ledger` و `journal_entries`.
8. **طويل المدى:** إزالة `LIKE 'ايداع%'` لصالح Enums.
